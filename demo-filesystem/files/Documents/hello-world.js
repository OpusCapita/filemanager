/* eslint-disable */
const express = require('express');
const app = express();

app.get('/', (req, res) => {
  res.send('Hello World!');
});

app.listen(3000, () => {
  console.debug('Listen on Port 3000 and Log...');
});
