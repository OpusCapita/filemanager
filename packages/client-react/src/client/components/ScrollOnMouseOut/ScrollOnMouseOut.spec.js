import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import ScrollOnMouseOut from '.';

describe('<ScrollOnMouseOut />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <ScrollOnMouseOut />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<ScrollOnMouseOut className="test-class-name" />);
    expect(wrapper).to.have.className('scroll-on-mouse-out');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
