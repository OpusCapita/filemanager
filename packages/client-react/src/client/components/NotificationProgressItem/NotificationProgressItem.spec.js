import React from 'react'; // eslint-disable-line
import { expect } from 'chai'; // eslint-disable-line
import { shallow } from 'enzyme'; // eslint-disable-line
import NotificationProgressItem from '.'; // eslint-disable-line

describe('<NotificationProgressItem />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <NotificationProgressItem />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<NotificationProgressItem className="test-class-name" />);
    expect(wrapper).to.have.className('notification-progress-item');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
