import React from 'react';
import ReactDOM from 'react-dom';
import { MultiUserAccessFileManager } from '@opuscapita/react-filemanager';

ReactDOM.render((<MultiUserAccessFileManager />), document.getElementById('main'));
