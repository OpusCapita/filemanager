import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import GridViewContainer from '.';

describe('<GridViewContainer />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <GridViewContainer />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<GridViewContainer className="test-class-name" />);
    expect(wrapper).to.have.className('grid-view-container');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
