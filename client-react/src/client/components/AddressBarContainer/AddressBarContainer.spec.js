import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import AddressBarContainer from '.';

describe('<AddressBarContainer />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <AddressBarContainer />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<AddressBarContainer className="test-class-name" />);
    expect(wrapper).to.have.className('address-bar-container');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
