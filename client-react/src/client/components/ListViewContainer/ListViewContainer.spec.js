import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import ListViewContainer from '.';

describe('<ListViewContainer />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <ListViewContainer />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<ListViewContainer className="test-class-name" />);
    expect(wrapper).to.have.className('list-view-container');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
