import React from 'react';
import { expect } from 'chai';
import { shallow } from 'enzyme';
import FolderLoadingStub from '.';

describe('<FolderLoadingStub />', () => {
  /* Recommended test-cases

  it('should have default props', () => {
    let component = <FolderLoadingStub />;
    expect(component.props.testProp).to.equal('Give me back my label!');
    expect(component.props.onClick).to.be.a('function');
  });
  it('should have the right class name', () => {
    let wrapper = shallow(<FolderLoadingStub className="test-class-name" />);
    expect(wrapper).to.have.className('folder-loading-stub');
    expect(wrapper).to.have.className('test-class-name');
  });

  */
});
