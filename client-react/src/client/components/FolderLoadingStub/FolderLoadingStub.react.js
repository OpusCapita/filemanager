import React, { Component, PropTypes } from 'react';
import './FolderLoadingStub.less';

const propTypes = {};
const defaultProps = {};

export default
class FolderLoadingStub extends Component {
  render() {
    return (
      <div className="oc-fm--folder-loading-stub">
      </div>
    );
  }
}

FolderLoadingStub.propTypes = propTypes;
FolderLoadingStub.defaultProps = defaultProps;
